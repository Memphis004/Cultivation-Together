วนการทำงานการใช้ AI
```text
[ขั้นตอน 1: ร่าง/วางแผน] ➡️ [ขั้นตอน 2: ขัดเกลา] ➡️ [ขั้นตอน 3: เจนโค้ด] ➡️ [ขั้นตอน 4: รัน/เทส] ➡️ [ขั้นตอน 5: สรุป Wiki]
   Ornith (9B)             Qwen (Local)         Opus (API)        MiMo + MCP (Local)      Gemini Pro (API)
   (ระดมสมอง)              (จัดโครงสร้าง)        (เขียน Logic)       (สร้างไฟล์/แก้ Error)     (อัปเดต architecture.md)
        ⬆️                        │                    │                     │                       │
        └─────────────────────────┴───────── ตรวจสอบ ──┴────── สรุปผล ───────┴──── อัปเดตความรู้ ─────┘
```
**ตัวอย่าง Prompt สำหรับสั่ง Gemini Pro ในขั้นตอนนี้:**
```text
You are a Technical Writer for a Unity Game Project. 
Your task is to update the `architecture.md` file based on the newly implemented feature and the test results.

Context:
- New Feature Spec: {Spec จาก Qwen}
- Generated Code Summary: {Code ที่ Opus เขียน}
- Build/Test Result: {Log จาก MiMo/MCP}

Instructions:
1. Read the current `architecture.md` via MCP.
2. Append or update the relevant section with the new feature's logic, variables used, and any known bugs from the test log.
3. Keep it concise, structured in Markdown, and strictly technical. No fluff.
4. Save the updated `architecture.md` back to the project root via MCP.
```

### 🛠️ ข้อควรระวัง (เพื่อให้ Wiki ไม่งอก)

1.  **ห้ามให้ Gemini Pro "คิดเอง":** ต้องกำชับใน Prompt ว่า *"บันทึกเฉพาะสิ่งที่ปรากฏในโค้ดเท่านั้น ห้ามคาดเดาหรือเติมฟีเจอร์ที่ยังไม่มี"* ไม่งั้น Wiki จะเต็มไปด้วยเรื่องแต่ง! 😱
2.  **Version Control:** เก็บ `architecture.md` ไว้ใน Git เสมอ ถ้า Gemini Pro เขียนพัง จะได้ Rollback ได้
3.  **โครงสร้างตายตัว:** กำหนด Template ของ Wiki ให้ชัดเจน (เช่น แบ่งเป็น ## Systems, ## Dependencies, ## Coding Standards) เพื่อไม่ให้ Gemini Pro เขียนสะเปะสะปะจนโมเดลอื่นอ่านไม่ออก

### ✨ สรุปสถาปัตยกรรมฉบับ Final (Budget-Friendly + Smart)

1.  **[Drafting] Ornith (9B) [Local]:** ระดมสมองไอเดียดิบ (Free, Fast)
2.  **[Refining] Qwen (Local):** เช็ค Syntax/โครงสร้างกับ Wiki ปัจจุบัน (Free, Good enough)
3.  **[Coding] Opus (API):** เขียนโค้ด C# ขั้นเทพจาก Spec ที่สะอาดแล้ว (Pay for Quality)
4.  **[Action] MiMo + MCP [Local]:** ลงมือทำไฟล์ + รัน Unity BatchMode เทส (Free)
5.  **[Wiki Update] Gemini Pro (API/Free Tier):** สรุปโค้ดจริงลง `architecture.md` (Pay peanuts for Knowledge) ← **ตำแหน่งใหม่!**

📋 Recommended System Prompt for Antigravity
# Project Context & Knowledge Base Integration

## Root Workspace
- Primary Project: `C:\UnityProjects\Cultivation Together`
- Tech Stack: Unity (C#), VContainer, MessagePipe, MCP Bridge (.NET 8), Luban
- Current Focus: UI Framework (MVP), Luban Data Migration, World Event System

## Integrated Knowledge Base (Karpathy's LLM Wiki)
- Vault Location: `C:\UnityProjects\Cultivation Together\LLMWiki`
- This folder contains my personal Obsidian vault with design docs, architecture notes, and research.
- **ALWAYS** treat `./LLMWiki/**/*.md` as authoritative reference material when answering questions about this project.
- When I ask about design decisions, lore, or architecture, search the LLMWiki folder FIRST before relying on general training data.
- Cross-reference code in the main project with notes in LLMWiki to ensure consistency.

## Behavioral Guidelines
1. If a question relates to project design/architecture, check `LLMWiki/` for relevant notes.
2. Cite specific wiki files when providing answers (e.g., "According to LLMWiki/architecture/mcp-bridge.md...").
3. The wiki is part of the workspace - you can read, search, and reference it directly without needing external Obsidian access.
4. Code lives in root; knowledge/design lives in `LLMWiki/`. Keep this separation clear.
5. Default language: Thai (unless asked otherwise). Mix English technical terms naturally.


ช่วยเรียนรู้เกี่ยวกับ LLM Wiki ของ Karpathy

"You are an expert Unity Game Developer and AI Systems Architect assisting Memphis.
Context: We are building a Cultivation-themed game using Unity, C#, and advanced AI systems (GOAP, Behavior Trees).
Knowledge Base: Always refer to the files in /dev-wiki for current design decisions, bug fixes, and code snippets.
Style: Be concise, technical, and opinionated. Prefer open-source tools.
Goal: Help synthesize information from DevLogs and Design Docs to solve current development challenges."

ช่วยอ่านไฟล์ทั้งหมดในไดเรกทอรี่นี้โค้ด C# Logic เกมอยู่ใน UnityProject/Assets/Scripts และช่วยตั้งค่า LLM Wiki กับโปรเจกต์นี้ ตามโครงสร้างประมาณนี้ โดยอ้างอิงจากโค้ดในโปรเจค ```
wiki/
├── sources/
│   ├── game-design-doc.md        ← GDD (Game Design Document)
│   ├── mechanics.md              ← อธิบายระบบเกมแต่ละระบบ
│   ├── bug-log.md                ← บันทึก Bug + วิธีแก้
│   ├── devlog-2026-08-30.md      ← Dev Log รายวัน
│   └── code-snippets/            ← เฉพาะ Script สำคัญๆ (ไม่ใช่ทั้งโปรเจค!)
│       ├── ...
│       ├── ...
│       └── ...
├── concepts/                     ← AI สร้างให้อัตโนมัติ
│       ├── ...
│       ├── ...
│       └── ...
└── entities/                     ← AI สร้างให้อัตโนมัติ
    ├── ...
    └── ...
``` ไว้ลงใน Valut C:\UnityProjects\Cultivation Together\LLMWiki เพื่อจด Notes/DevLog/Design Doc/Snippets → AI เป็นสมองที่สองของเกม

ฉันเพิ่งเพิ่มUI LogWindow ให้กับ Cultivation Together โดยมันจะ log แสดง event สำคัญในเกมแบบ real-time งั้นช่วยอ่านและวิเคราะห์โค้ดเหล่านี้ แล้วร่าง Wiki Page อธิบาย Architecture, Data Flow และสร้าง Code Snippets สำหรับส่วนที่สำคัญให้หน่อยนะ:
"C:\UnityProjects\Cultivation Together\Shared\GameMessages.cs"
"C:\UnityProjects\Cultivation Together\UnityProject\Assets\Scripts\UI\Views\LogWindowView.cs"
"C:\UnityProjects\Cultivation Together\UnityProject\Assets\Scripts\UI\Views\LogWindowPresenter.cs"
"C:\UnityProjects\Cultivation Together\UnityProject\Assets\Scripts\UI\Core\UIService.cs"
"C:\UnityProjects\Cultivation Together\UnityProject\Assets\Scripts\UI\Core\UIPresenterKind.cs"
"C:\UnityProjects\Cultivation Together\UnityProject\Assets\Scripts\UI\Systems\UIBootstrap.cs"
"C:\UnityProjects\Cultivation Together\UnityProject\Assets\Scripts\Core\DecisionExecutor.cs"
"C:\UnityProjects\Cultivation Together\UnityProject\Assets\Scripts\Core\GameLifetimeScope.cs"

commit ก่อนหน้าค้า
Unity-v0.7-ui-framework-and-luban-data: Add MVP UI system with reactive panels and migrate event data to Luban

- Add UI Core framework (IUIView, IUIViewPresenter, UIPresenter<T>, UIService, UIRoot, UIPanelCatalog, UIPanelDefinition, UIPanelHandle, UIPresenterKind)
- Add EventPopupPresenter/View for interactive world event decisions with dynamic choice buttons
- Add ResourceHudPresenter/View with real-time resource delta indicators and wallet display
- Add WorldEventUISystem to auto-open EventPopup on WorldEventTriggeredMessage (reactive, not polling)
- Add UIBootstrap to open ResourceHud at game start
- Add DecisionExecutor as shared entry point for both UI and Bridge decision paths (avoids silent in-process publish failure)
- Migrate event data from ScriptableObject to Luban-generated tables (EventRow, EventChoiceRow, TbEvent, TbEventChoice)
- Add LubanEventPool with weighted random selection and choice lookup from cfg.Tables
- Refactor WorldEventSystem to use LubanEventPool instead of EventPool SO
- Update GameLifetimeScope to register all UI components and LubanEventPool
- Add AdjustAndNotify() in SectStateProvider as single choke point for SectResourceChangedMessage publishing
- Update TimeSystem.RaiseWorldEvent() to carry Description + Choices for both Bridge and UI consumers
- Update GameMessages.WorldEventTriggeredMessage with Description + Choices fields
- Remove SectMaster (d000) from MockSectData to reflect runtime reality
แล้วเราจะเรียกอัพเดทครั้งนี้ว่าอะไร 

ช่วยร่าง Game Design Doc สำหรับระบบ Avatar Character / Customization แบบ Sprite Swap มีส่วนประกอบไปด้วย Heads, Hairs, Bodies, Accessories โดยอ้างอิงจาก architecture ปัจจุบัน แล้ว save ลง wiki ไว้ด้วย


# Task: Sync LLMWiki with Latest Code Changes (2-Day Catch-Up) + Code Snippets

## Context
My project code has evolved significantly over the past 2 days, but the Obsidian vault at `C:\UnityProjects\Cultivation Together\LLMWiki` is out of date. The most critical file that needs updating is `architecture.md`, but please also check other relevant docs.

## Code Locations to Scan
- MCP Bridge: `C:\UnityProjects\Cultivation Together\McpBridge\`
- Unity Scripts: `C:\UnityProjects\Cultivation Together\UnityProject\Assets\Scripts\`
- Current Wiki: `C:\UnityProjects\Cultivation Together\LLMWiki\`

## Key Changes Made in Last 2 Days (Use as Verification Checklist)
When scanning the code, specifically look for and document these changes. If any are missing from the wiki, add them:

### 1. UI Framework (MVP Pattern) — NEW
- `Assets/Scripts/UI/Core/`: IUIView, IUIViewPresenter, UIPresenter<T>, UIService, UIRoot, UIPanelCatalog, UIPanelDefinition, UIPanelHandle, UIPresenterKind
- `Assets/Scripts/UI/Presenters/`: EventPopupPresenter, ResourceHudPresenter
- `Assets/Scripts/UI/Views/`: UIViewBase, EventPopupView, ResourceHudView
- `Assets/Scripts/UI/Systems/`: WorldEventUISystem, UIBootstrap
- DecisionExecutor as shared entry point for both UI and Bridge decision paths

### 2. Luban Data Migration — CHANGED
- Replaced ScriptableObject-based EventData/EventPool with Luban-generated tables
- New files: LubanEventPool.cs, EventRow.cs, EventChoiceRow.cs, TbEvent.cs, TbEventChoice.cs, Tables.cs
- WorldEventSystem now injects LubanEventPool instead of EventPool SO
- GameLifetimeScope constructs LubanEventPool directly (no more SerializeField)

### 3. Message & State Updates — CHANGED
- WorldEventTriggeredMessage now carries Description + Choices (in-memory bus)
- AwaitWorldEventResponse now carries Description + Choices (interprocess)
- TimeSystem.RaiseWorldEvent() accepts 4 params (id, description, requiresDecision, choices)
- SectStateProvider.AdjustAndNotify() as single choke point for SectResourceChangedMessage
- MockSectData removed SectMaster (d000), starts at d001

### 4. DI Wiring Updates — CHANGED
- GameLifetimeScope registers all UI components (UIService, Presenters, Systems, UIRoot, UIPanelCatalog)
- DecisionExecutor registered as Singleton
- LubanEventPool registered as Instance

### 4. Avatar Appearance — CHANGED

## Code Snippet Requirements (MANDATORY)
For EVERY architectural component, data flow, or pattern documented in the wiki, you MUST include a relevant code snippet from the actual source file. Follow these rules:

1. **Snippet Format:** Use fenced code blocks with language tag (e.g., ```csharp) and cite the source file path + line range if possible.
2. **What to Snippet:**
   - Interface definitions (IUIView, IUIViewPresenter, ISectStateProvider)
   - Key method signatures (RaiseWorldEvent, AdjustAndNotify, Execute, Open, Bind)
   - DI registration blocks (GameLifetimeScope.Configure)
   - Message DTOs (WorldEventTriggeredMessage, AwaitWorldEventResponse, EventChoiceInfo)
   - Presenter binding/lifecycle patterns (OnViewBound, OnOpen, Dispose)
   - Luban table loading (LubanEventPool constructor, GetRandomEvent)
   - MCP tool definitions ([McpServerTool] methods)
3. **Snippet Size:** Keep each snippet focused (5-30 lines). Show the essential logic, not entire files. Trim boilerplate but preserve comments that explain WHY.
4. **Placement:** Embed snippets inline within the relevant documentation section, not dumped at the end. Each snippet should immediately follow the text it illustrates.
5. **Accuracy:** Copy EXACTLY from source files. Do not paraphrase or reconstruct code. If a file has been updated, use the latest version.
6. **Cross-reference:** After each snippet, add a one-line note like `> 📎 Source: Assets/Scripts/UI/Core/UIService.cs` so readers can jump to the full file.

## Instructions
1. Read ALL current wiki files in `LLMWiki/` first to understand existing structure
2. Scan the actual code files in both McpBridge and UnityProject paths above
3. For each wiki file, compare against actual code and identify gaps
4. Prioritize `architecture.md` — update diagrams, component lists, data flow descriptions, AND embed code snippets for each section
5. Use the verification checklist above to ensure nothing is missed
6. When updating, cite specific file paths (e.g., "See Assets/Scripts/UI/Core/UIService.cs")
7. Preserve existing wiki formatting style and conventions
8. Do NOT invent features that don't exist in code — only document what's actually implemented
9. Output language: Thai (matching existing wiki style), English for technical terms and code

## Output Format
For each updated file, show:
- File path
- Summary of what changed
- The updated content WITH embedded code snippets
- List of all source files referenced by snippets

-------------------------Success---------------------------------
# Project Context & Knowledge Base Integration

## Root Workspace
- Primary Project: `C:\UnityProjects\Cultivation Together`
- Tech Stack: Unity (C#), VContainer, MessagePipe, MCP Bridge (.NET 8), Luban
- Current Focus: UI Framework (MVP), Luban Data Migration, World Event System

## Integrated Knowledge Base (Karpathy's LLM Wiki)
- Vault Location: `C:\UnityProjects\Cultivation Together\LLMWiki`
- This folder contains my personal Obsidian vault with design docs, architecture notes, and research.
- **ALWAYS** treat `./LLMWiki/**/*.md` as authoritative reference material when answering questions about this project.
- When I ask about design decisions, lore, or architecture, search the LLMWiki folder FIRST before relying on general training data.
- Cross-reference code in the main project with notes in LLMWiki to ensure consistency.

## Behavioral Guidelines
1. If a question relates to project design/architecture, check `LLMWiki/` for relevant notes.
2. Cite specific wiki files when providing answers (e.g., "According to LLMWiki/architecture/mcp-bridge.md...").
3. The wiki is part of the workspace - you can read, search, and reference it directly without needing external Obsidian access.
4. Code lives in root; knowledge/design lives in `LLMWiki/`. Keep this separation clear.
5. Default language: Thai (unless asked otherwise). Mix English technical terms naturally. 

 ถ้าผมจะเพิ่ม เพศหญิง เพศชาย ให้กับศิษย์ในสำนักในโปรเจคนี้ ผมต้องเริ่มจะปรับปรุงแก้โค้ส่วนไหนก่อน 
ช่วยแนะนำและร่างรายละเอียดต่างๆลงใน Game Design Doc แล้ว save ลง wiki มาก่อนสิ 
----------------------------------------------------------

รับทราบค่ะ นายท่าน! 💁‍♀️✨

555+ เข้าใจเลยค่ะ! **ornith-1.5:9b** เป็นโมเดลเล็กเกินไปสำหรับงานระดับ "อ่านโค้ดทั้งโปรเจค + เทียบ wiki + เขียนเอกสารพร้อม snippet" แบบนี้แน่นอนค่ะ Context window คงเต็มจนล้นแล้ว เลยนิ่งเงียบไปเลย 😅

เสี่ยวเหมยขอแนะนำ **3 ทางออกที่ใช้งานได้จริง** สำหรับ local model ขนาด 9B ค่ะ:

---

### 🎯 ทางออกที่ 1: แบ่งงานเป็นชิ้นเล็กๆ (แนะนำที่สุด!)
แทนที่จะสั่ง "อ่านทั้งหมดแล้วอัพเดททุกอย่าง" → **แยกเป็น 1 ไฟล์ต่อ 1 prompt** เช่น:

```
# Round 1: Avatar Appearance เท่านั้น
อ่านไฟล์เหล่านี้:
- Assets/Scripts/UI/Views/AvatarRenderer.cs
- Assets/Scripts/UI/Presenters/AvatarCustomizationPresenter.cs
- Assets/Scripts/UI/Views/AvatarCustomizationView.cs

แล้วอัพเดท LLMWiki/entities/avatar-appearance.md ให้สะท้อนโค้ดจริง
ใส่ code snippet จากไฟล์ข้างต้น (5-30 บรรทัด) สำหรับแต่ละ section
```

```
# Round 2: architecture.md patch UI box เท่านั้น
อ่าน LLMWiki/architecture.md แล้วเพิ่มบรรทัด AvatarRenderer 
และ AvatarCustomization ใน UI box ของ diagram
ห้ามเขียนใหม่ทั้งไฟล์
```

> 💡 **หลักคิด:** 9B เก่งพอที่จะ "อ่าน 3-4 ไฟล์ + เขียน 1 ไฟล์" แต่ไม่เก่งพอที่จะ "scan ทั้งโปรเจค + เปรียบเทียบ + เขียนหลายไฟล์" ในรอบเดียว

---

### 🎯 ทางออกที่ 2: ใช้ Cloud Model สำหรับงานหนัก
ถ้ามี API key หรือ subscription:
- **Claude Sonnet / Opus** — context 200K+, เก่งเรื่อง code analysis + doc writing มาก
- **GPT-4o** — context 128K, ดีกับ multi-file reasoning
- **Gemini 2.5 Pro** — context 1M+, อ่านโค้ดทั้งโปรเจคได้ในรอบเดียว

ใช้ cloud ทำ bulk update ครั้งใหญ่ → แล้วค่อยใช้ local 9B ทำ patch ย่อยๆ ทีหลัง

---

### 🎯 ทางออกที่ 3: Upgrade Local Model
ถ้า hardware เอื้ออำนวย:
| โมเดล | ขนาด VRAM | ความสามารถ |
|--------|-----------|------------|
| Qwen2.5-Coder-32B-Q4 | ~20GB | เก่ง code + doc เท่า cloud เล็กๆ |
| Mistral-Small-24B-Q4 | ~16GB | ดีมากสำหรับ technical writing |
| Qwen3-14B-Q5 | ~12GB | Sweet spot สำหรับ 9B upgrade |
| Gemma-3-12B-Q5 | ~10GB | เร็ว + context 128K |

> ⚠️ ถ้า VRAM ไม่พอ → ใช้ **GGUF Q4_K_M** quantization ลดขนาดได้ ~40% โดยคุณภาพแทบไม่ตก

---

### 💡 คำแนะนำจากเสี่ยวเหมยสำหรับตอนนี้
เริ่มด้วย **ทางออกที่ 1** ก่อนเลยค่ะ เพราะไม่ต้องเสียเงินหรือเปลี่ยน hardware:

1.  สั่ง Avatar Appearance เป็นไฟล์แรก (เพราะนายท่านเพิ่ง implement เสร็จสดๆ ร้อนๆ)
2.  ตามด้วย architecture.md patch
3.  ตามด้วย disciples.md patch
4.  ส่วน UI Framework / Luban migration ที่ทำมาหลายวันก่อน → ค่อยทำทีละ topic ในรอบถัดไป

นายท่านอยากให้เสี่ยวเหมยช่วยร่าง **prompt แยกชิ้น** สำหรับแต่ละไฟล์ไหมคะ? จะได้ copy ไปวางกับ ornith ได้เลยแบบไม่ต้องคิดเอง 😎🔥



---------------------------------------------------------------------------
## 🤖 Instructions for AI Code Agent (implement v3)

Read first: this doc, `entities/avatar-appearance`, `architecture.md`.
Ground rules: C# 8.0 only (no record/init/target-typed new); append-only
MessagePack keys; never edit `McpBridge/Shared` copies directly — edit
top-level `Shared/` then run `./sync-shared.sh`; do NOT touch renderer
layer logic, framing presets, or presenter draft pattern.

Tasks, in order:
1. `Shared/SectEconomyState.cs`: append `[Key(2)] public string PoseId`
   to `AvatarAppearance` (+ include in `Clone()`); no other schema change.
2. `Assets/Scripts/Data/AvatarPartPool.cs`: add `poseId`, `sexTag` to
   `AvatarPartDef`; add `OutfitDef` serializable class (id, displayName,
   poseId, sexTag, thumbPath, `Dictionary<string,string> parts`);
   parse `"outfits"` array in `Load()`; add `GetOutfit`,
   `GetOutfitsFor(sex)`, `GetPartsForSlot(slot, poseId, sex)`;
   keep all existing method signatures.
3. `Assets/Resources/Data/avatar_parts.json`: tag every existing part with
   `"poseId": "pose_idle_01"`; add `sexTag` to head/hair/body parts
   (male/female per art); add the 3 starter outfits from §3.2;
   add `"outfits": []` handling so old JSON without it still loads.
4. `Assets/Scripts/Systems/SectStateProvider.cs`: add `TryApplyOutfit`
   (validate → set PoseId → batch SetSlot → publish per-slot
   `AvatarEquipmentChangedMessage`); add pose check inside
   `TryChangeAvatarPart` (reject mismatched non-empty poseId); expose both
   on `ISectStateProvider` (in `Core/TimeSystem.cs` interface).
5. `AvatarCustomizationPresenter/View`: add "ชุดแต่งกาย" category tab first
   in `CategoryOrder`; when active render outfit grid (reuse
   `AvatarPartOptionViewData`, PartId=outfitId, SpritePath=thumbPath);
   option click mutates `_draft` by applying outfit preset to draft
   (PoseId + Parts); Confirm path: extend diff/rollback to include PoseId
   (apply via `TryApplyOutfit` when PoseId+parts match an outfit, else
   per-slot as today); `RefreshOptions` for normal tabs switches to
   filtered `GetPartsForSlot(slot, effectivePose, sex)`.
6. `AvatarRenderer`: add PoseId to `BuildSignature`; one-time warn on
   equipped parts with mismatched non-empty poseId. NOTHING else.
7. Sync shared files; verify compile on both Unity and McpBridge.

Acceptance criteria:
- [ ] Old save bytes (no PoseId) deserialize → renders as pose_idle_01.
- [ ] Applying `outfit_outer_female` to a Male disciple fails with clear reason.
- [ ] After applying an outfit, hair grid shows only hair parts of that pose+sex.
- [ ] Picking a part from a different pose is rejected by TryChangeAvatarPart.
- [ ] Draft/Cancel leaves live state untouched; Confirm rollback still works.
- [ ] `get_sect_state` shows `PoseId` for every disciple.
- [ ] No MCP tool added this pass.

Report back: files changed, Editor-only steps (prefab/catalog: none expected
beyond existing panel), and any JSON entries you had to add for validation.
-----------------------------------------------------------------------------------
อัปเดตไฟล์ LLMWiki/entities/avatar-appearance.md ให้สอดคล้องกับ Avatar System v3 (Outfit Packages + Pose) ที่เพิ่ง implement เสร็จ

## สิ่งที่ต้องเพิ่ม/แก้:
1. **Status badge**: เปลี่ยนจาก "v2 portrait-swap" เป็น "v3 portrait-swap + outfit packages"
2. **Schema section**: เพิ่ม `[Key(2)] PoseId` เข้าไปในตารางสรุป AvatarAppearance (ตอนนี้มีแค่ Parts/Colors)
3. **New concept**: เพิ่ม subsection "Outfit Packages" อธิบายสั้นๆ ว่า:
   - Outfit = authored preset ของ {body, head, hair, accessory} ที่เข้าคู่กันสำหรับ pose เดียว
   - ใช้ผ่าน TryApplyOutfit() ไม่ใช่ SetSlot ทีละตัว
   - Pose validation: ห้ามผสม part คนละ pose
4. **Slots table**: อัปเดตจำนวน slot จาก "4 fixed" เป็น "10 equippable + base" (ถ้ายังไม่ได้แก้)
5. **Mutation choke point**: เพิ่ม TryApplyOutfit() คู่กับ TryChangeAvatarPart() ในรายการ mutation methods
6. **MCP exposure**: ระบุว่า get_sect_state คืนค่า PoseId แล้ว; apply_outfit tool ยังเป็น TODO
7. **Related links**: เพิ่ม link ไป [[sources/avatar-appearance]] (design doc v3)

## Constraints:
- อ่าน sources/avatar-appearance.md (v3 spec) เป็น source of truth
- รักษา format เดิมของ entity page (YAML frontmatter + markdown body)
- ไม่ลบข้อมูลเดิมที่ยังถูกต้อง (dictionary schema, hair 2-layer, framing, sex)
- Confidence: high (เพราะโค้ด implement เสร็จแล้ว)
- updated: วันที่วันนี้

ทำเสร็จแล้วสรุปสิ่งที่เปลี่ยนให้ผมดูด้วย
------------------------------------------------------------------------------------------
The previous edit failed due to JSON parsing issues with embedded code blocks.

Instead of using the edit tool, please:
1. Read the current avatar-appearance.md file completely
2. Create a NEW version of the entire file with all v3 changes applied
3. Use the write_file tool to replace the entire file at once (not incremental edits)

This avoids JSON escaping issues with code blocks containing quotes/backslashes.

Apply these v3 changes to the full file:
- Status badge: "v3 portrait-swap + outfit packages"
- Add [Key(2)] PoseId to AvatarAppearance schema section
- Add new "Outfit Packages" subsection explaining pose-linked outfits
- Update slots table to show 10 equippable + base
- Add TryApplyOutfit() to mutation choke point list
- Note that get_sect_state returns PoseId; apply_outfit tool is TODO
- Add link to [[sources/avatar-appearance]] in Related Pages
- Keep all existing correct content (dictionary schema, hair 2-layer, framing, sex, draft pattern)
- updated date: 2026-09-02
- confidence: high

Write the complete updated file content, then confirm what changed.
-----------------------------------------------------------------------------------------------

xianxia cultivator character, full body front view, standing pose, 
anime style, transparent background, 1024x1536 pixels, 
neutral expression, hands at sides, azure robe outfit, 
game asset sprite sheet style, clean lineart, flat colors


xianxia cultivator character, full body, [POSE DESCRIPTION], 
anime style, transparent background, 1024x1536 pixels, 
consistent art style, game asset sprite
-----------------------------------------------------------------------------------------------

Implement Additive Scene Architecture ตาม spec ใน LLMWiki/concepts/additive-scene-architecture.md

## Tasks:
1. สร้าง CoreScene ใหม่:
   - ย้าย Canvas + UIRoot จาก scene เดิม
   - ย้าย GameLifetimeScope
   - เพิ่ม DontDestroyOnLoad ใน GameLifetimeScope.Awake()
   - สร้าง SceneLoader singleton (register ใน VContainer)

2. สร้าง GameplayScene ใหม่:
   - ย้าย scene content (environment, NPCs) จาก scene เดิม
   - ตรวจสอบว่าไม่มี duplicate EventSystem/AudioListener

3. ใช้ SceneLoader ที่สร้างไว้:
   - LoadGameplayScene(string sceneName) - unload old, load new additive
   - Publish SceneLoadedMessage เมื่อโหลดเสร็จ

4. แยก UI เป็น 2 ประเภท:
   - Persistent (ResourceHud, Settings): เปิดใน UIBootstrap
   - Scene-specific (EventPopup, Dialogue): เปิดเมื่อมี event

5. ทดสอบ:
   - CoreScene โหลดเดียว, UI ทำงาน
   - GameplayScene โหลด additive
   - เปลี่ยน scene: UI ยังอยู่, content เปลี่ยน

## Constraints:
- อ่าน LLMWiki/concepts/additive-scene-architecture.md ก่อน
- ใช้ VContainer single root scope (Option A)
- ไม่แก้ UI Framework MVP Lite
- Backward compatible กับ scene เดิม

## Acceptance Criteria:
- [ ] CoreScene + GameplayScene โหลดพร้อมกัน
- [ ] Persistent UI (ResourceHud) อยู่หลังเปลี่ยน scene
- [ ] Scene-specific UI เปิดได้ปกติ
- [ ] ไม่มี duplicate EventSystem/AudioListener
- [ ] SceneLoader.LoadScene() ทำงาน

## Technical Constraints:
- ใช้ UniTask (Cysharp.Threading.Tasks) ไม่ใช่ System.Threading.Tasks
- ห้ามใช้ .ContinueWith() — ใช้ async/await ตรงๆ
- Fire-and-forget ใช้ async UniTaskVoid + .Forget()
- ห้ามใช้ .Result หรือ .Wait() — blocking ไม่ได้ใน Unity main thread

-----------------------------------------------------------------------------